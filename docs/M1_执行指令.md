# PodLore · M1 执行指令（给 Trae）

> 前置状态：M0 ✅ 已通过验收（评审记录见 docs/M0_验收评审记录.md）
> 任务：执行 M1「数据抓取层」——请先读本指令，再执行开发指令文档的 M1 章节

---

## 一、M0 放行确认（先完成这两项，再开始 M1）

1. **确认 paraformer-zh（990MB）已从 data/models/ 清理**——这是 M0 修正清单的遗留项，请确认删除完成
2. 确认 3 个模型（SenseVoiceSmall + fsmn-vad + bge-small-zh-v1.5 的惰性加载配置）实际存放位置在 data/models/（gitignore 内，不入库）

**确认后回复：「M0 遗留项已确认，开始 M1」**，再执行 M1。

---

## 二、M1 任务概览（详见开发指令文档 M1 章节）

**目标**：粘贴小宇宙分享链接 → 抓取页面 → 解析 `__NEXT_DATA__` → 元数据 + 音频直链入库。

### 交付文件
1. `backend/app/infra/fetch_xyz.py`——URL 解析 / 页面抓取 / __NEXT_DATA__ 提取 / 元数据归一化 / 封面提取
2. `backend/app/infra/downloader.py`——音频流式下载（断点续传 + 校验）
3. `backend/app/infra/db.py`——建表 + 幂等 CRUD（podcasts / episodes / transcript_paras）

### M1 验收清单（完成后逐项勾选）
- [ ] 用真实小宇宙链接解析出全部元数据（title / audio_url / duration / shownotes / 热度字段 / cover / series_name 提取）
- [ ] 音频直链下载到 data/audio/（断点续传可用）
- [ ] episodes 表幂等入库（重复导入不产生重复行）
- [ ] pytest 全绿

---

## 三、M1 执行要点（Trae 注意）

1. **已实测的数据源事实**（不必重新验证，直接实现）：
   - 小宇宙单集页 URL 格式：`https://www.xiaoyuzhoufm.com/episode/{eid}`
   - 页面含 `__NEXT_DATA__` script，`props.pageProps.episode` 内含全部字段（eid/pid/title/description/duration/enclosure.url/shownotes/podcast/playCount/clapCount/favoriteCount/commentCount/pubDate）
   - 音频直链在 `enclosure.url`（m4a 格式）
   - 封面：优先 `episode.podcast.cover`，兜底页面 `og:image`
   - 无需登录，GET + 浏览器 UA 即可（超时 15s）
2. **series_name 提取**（本 M 做基础版）：从标题按 `EP{n} {系列}｜{副标题}` 规则提取，存 episodes.series_name（第二期 M9 再完善）
3. **幂等设计**：episodes 表 eid 唯一；重复导入时更新字段不新增行
4. **测试 fixture**：抓取 1 个真实页面 HTML 存 `backend/tests/fixtures/`（避免测试依赖网络），用 fixture 测解析逻辑
5. **遇到小宇宙页面结构变化**：不要硬适配，报告差异 + 贴出实际结构，等决策

---

## 四、交付方式

- M1 完成后输出：差异清单 + pytest 测试结果 + 验收清单勾选
- 卡住时：输出「问题 + 可选方案 + 建议」，等确认后继续
- **不要一次做完多个 M**：M1 验收通过后再进入 M2
