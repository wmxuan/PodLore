# PodLore · M1 验收评审记录

> 评审人：王敏旋 ｜ 评审日期：2026-08-31 ｜ 状态：✅ 通过（放行 M2）
> 依据：Trae M1 完成报告（b541890）+ 开发指令 M1 验收清单 + M0 评审记录

---

## 一、M1 验收结论

**判定：✅ 通过**——数据抓取层完整交付，实测端到端验证通过。

| 验收项 | 结果 | 证据 |
|---|---|---|
| 真实链接解析全部元数据 | ✅ | 声动早咖啡 897s 单集：title/audio_url/duration/pub_date/cover/热度四字段/shownotes/series_name 全部到位 |
| 音频下载到 data/audio/ | ✅ | 断点续传实测（5MB 中断→续传完成 14,523,310 bytes == 远端大小）|
| episodes 表幂等入库 | ✅ | 两次导入 id1==id2，行数不增，字段更新 |
| pytest 全绿 | ✅ | 32 passed（6.37s），覆盖 URL/页面/封面/DB/下载/端到端 |

---

## 二、M1 差异清单评审

### 实现增强类（✅ 合理）
| 差异 | 评审 |
|---|---|
| fetch_episode(url_or_eid) 便捷入口 | ✅ 供后续 API/CLI 复用 |
| db.py 顺带 upsert podcasts 表 | ✅ 防节目数据丢失，合理 |
| downloader 处理 416（幂等）与无 Range 服务器（200 重头覆盖）| ✅ 边界处理扎实 |
| PODLORE_DB 环境变量（测试隔离）| ✅ 工程规范 |

### 实测纠正文档错误（✅ 亮点，按协作规范报告而非硬适配）
| 差异 | 评审 |
|---|---|
| 封面字段：文档写 `episode.podcast.cover`，实际不存在 → 真实结构 `podcast.image.picUrl`（690×690），og:image 兜底照做（实测两者一致）| ✅ 正确报告并修正 |
| 音频直链：enclosure.url 与 media.source.url 同值，取前者兜底后者 | ✅ 合理 |
| fixture 用真实单集页（177KB）| ✅ 测试不依赖网络 |

---

## 三、遗留与提醒

- 无遗留阻塞项
- M2 前置事实（M0 已预验证）：SenseVoiceSmall + fsmn-vad 两模型方案，RTF 0.17（300s 音频约 53s CPU 全流水线）——M2 直接正式化，不必重新验证

---

## 四、M2 放行决定

**M1 ✅ 通过 → 放行 M2（本地转写流水线）**。M2 按开发指令 + docs/M2_执行指令.md 执行。
