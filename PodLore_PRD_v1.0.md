# PodLore PRD v1.0 —— 把播客变成你的书

> 版本：v1.0 ｜ 日期：2026-08-31 ｜ 作者：王敏旋
> 状态：待评审
> 标注：✅=已实测验证 ｜ 🔵=待验证/推断
> 产品一句话：把 1 小时播客音频，变成你能读、能改、能标注、能检索的「书」。

---

## 1. 背景与目标

### 1.1 背景
- 播客是「听过就忘」的媒介：1-2 小时音频，通勤/开车场景收听，无法搜索、无法快速回看、听完难沉淀
- 用户（本人）是播客重度用户（小宇宙），真实痛点：① 移动场景听到好内容不方便记录 ② 听过但找不到 ③ 节目太长/语速快/口齿不清，听感差
- 市面上竞品（Podwise 等）做「转写+摘要+推送笔记」，但**只解决提取，不解决沉淀**——笔记推送走后与原文割裂，用户无法像读书一样回看、标注、再消费

### 1.2 产品定位
**把播客变成你的书**：播客转写稿本质上就是「一本有声书的内容」——用户可以把转写稿精编成书，加入书架，像用微信读书一样划线、标注、写笔记，让「听过的」变成「读过的、思考过的、找得到的」。

### 1.3 目标用户
- 核心：C 端播客重度听众（知识型播客受众：商业/科技/历史/人文）
- 画像：每天通勤听 1-2 小时，想沉淀知识但苦于「听完就忘」
- 用户本人即典型用户（自用 + 求职作品）

### 1.4 成功指标
| 层级 | 指标 | 说明 |
|---|---|---|
| 北极星 | **用户主动沉淀数**（用户产生的划线+笔记+纠错总量）| 不是 AI 生成的摘要——是用户「主动加工」的知识单元，直接度量「沉淀发生」。不强迫阅读，用 AI 摘要、划线、笔记、纠错任何一种主动行为都算 |
| 过程 | 导入转化率（链接→成功转写）、成书率（转写稿→加入书架）、沉淀转化率（阅读/标注/检索的使用）| 沉淀链路各环节 |
| 护栏 | 转写失败率、语义搜索无结果率、用户放弃编辑率 | 体验底线 |

---

## 2. 需求范围

### 2.1 In Scope（两期）
- 第一期：链接导入 → FunASR 转写 → AI 加工（摘要/金句/章节）→ 精编（AI 去广告+手动改写+纠错）→ 成书 → 阅读（划线/笔记/选中搜索）→ **自建播放器+转写稿同步（边听边读边标注）** → 语义搜索
- 第二期：多 Agent 协作（系列研究/观点对比/知识卡片）→ 系列关联分析 → 热度分析 → 标注数据反哺

### 2.2 Out of Scope（明确不做）
- ❌ 不做播客播放器（收听用原 App，PodLore 只管「转写稿→书」）
- ❌ 不做社交/分享（C 端个人工具，标注不公开）
- ❌ 不做对外 MCP Server（C 端产品无需开放生态接口，内部用 Function Calling）
- ❌ 不做批量爬虫/全网音频采集（只处理用户主动粘贴链接的单集）
- ❌ 不做全平台播客接入（第一期仅支持小宇宙分享链接，RSS 后续再加）

---

## 3. 用户故事

### 第一期（P0）
| ID | 用户故事 | 优先级 |
|---|---|---|
| US-01 | 作为用户，我在小宇宙看到感兴趣的节目，复制分享链接粘贴到 PodLore，它能自动抓取节目信息并开始转写 | P0 |
| US-02 | 作为用户，我能看到转写进度，完成后自动生成摘要、大纲、金句 | P0 |
| US-03 | 作为用户，我能编辑转写稿：一键删除 AI 标记的广告段落、手动改写/纠错 ASR 错字、删掉我不喜欢的内容 | P0 |
| US-04 | 作为用户，我编辑完成后点「加入书架」，它变成一本结构化的书 | P0 |
| US-05 | 作为用户，我能像微信读书一样阅读这本书：划线、加笔记、查看标注列表 | P0 |
| US-06 | 作为用户，我能搜索「我听过/读过什么」——输入「护城河」能找到相关片段（语义搜索） | P0 |
| US-07 | 作为用户，我能管理书架（书的列表、删除、重新编辑） | P1 |

### 第二期（P1）
| ID | 用户故事 | 优先级 |
|---|---|---|
| US-08 | 作为用户，我输入「研究 Mini MBA 系列讲了哪些营销理论」，多 Agent 自动完成研究并输出报告 | P1 |
| US-09 | 作为用户，我输入「各集关于定价策略分别怎么说的」，得到跨集观点对比 | P1 |
| US-10 | 作为用户，我选择一本书/系列，AI 生成知识卡片存入知识库 | P1 |
| US-11 | 作为用户，我能看到系列的关联路径（脉络导览/主题索引），按图索骥收听 | P1 |
| US-12 | 作为用户，我能看到系列内哪些集最火（热度数据） | P1 |
| US-13 | 作为用户，我的划线/笔记被用来优化金句提取（数据飞轮） | P2 |

---

## 4. 方案设计

### 4.1 核心流程（第一期）

```
① 导入：用户粘贴小宇宙分享链接
   → 抓取页面 ✅ → 解析 __NEXT_DATA__ ✅ → 得到元数据+音频直链 ✅
② 转写：下载音频 → FunASR/Paraformer 中文转写（本地）→ 分段转写稿
③ 加工：LLM 生成摘要/大纲/金句（带时间戳）→ AI 标记广告段落
④ 精编（可选，不强制）：AI 去广告建议 → 用户一键删除/保留
   → 用户手动改写/纠错 → 「加入书架」
⑤ 成书：冻结快照（章节/段落结构）→ 存入书架（封面复用播客封面 ✅）
⑥ 阅读：自建播放器（音频直链 ✅）+ 转写稿同步 → 边听边读
   → 划线 + 笔记 + 选中搜索（标注附加搜索结果）
⑦ 语义搜索：本地 embedding + 向量检索（跨书召回片段）
```

### 4.2 页面结构

| 页面 | 功能 | 期 |
|---|---|---|
| ① 首页「知识宇宙」 | **四模块**：知识词云（点击直达语义搜索）+ 学习足迹（热力图）+ 书架画廊（最近的书）+ 数据成就（沉淀数）| 1 |
| ② 节目列表 | 已导入节目、转写状态、快捷入口 | 1 |
| ③ 转写加工页 | 转写进度、摘要/大纲/金句展示、AI 广告标记 | 1 |
| ④ 精编页 | AI 建议列表（可删/可留）+ 全文编辑（改写/纠错）+ 「加入书架」 | 1 |
| ⑤ 书架页 | 已成的书（封面/标题/进度/章节数）| 1 |
| ⑥ 阅读器页 | 读书（章节/段落/进度条）+ 划线 + 笔记（微信读书式）+ **自建播放器（音频直链）+ 转写稿同步高亮** | 1 |
| ⑦ 标注列表页 | 我的全部划线/笔记（按书聚合，可回跳原文）| 1 |
| ⑧ 语义搜索页 | 搜索框 + 结果列表（来源书/章节/片段，带上下文）| 1 |
| ⑨ 设置页 | 转写引擎/LLM Key/存储路径 | 1 |
| ⑩ 系列页（第二期） | 系列列表 + 关系网络可视化 + 关联路径 | 2 |
| ⑪ 主题索引页（第二期） | 主题 → 相关片段/集数 | 2 |
| ⑫ 研究台（第二期） | 多 Agent 任务输入 → 研究报告/观点对比/知识卡片 | 2 |

### 4.3 关键交互设计

**阅读器（对标微信读书 + 边听边读）**：
- **自建播放器 + 转写稿同步**：用抓取的音频直链（✅ m4a）做 HTML5 播放器；播放时转写稿同步高亮当前段落；可边听边读边标注——不内嵌小宇宙网页（规避 CSP 限制，交互一体化）
- 划线：选中文本 → 划线（支持多段）→ 弹出「加笔记」输入
- **选中搜索**（对标微信读书）：选中文本 → 操作条：划线/笔记/搜索 →「搜索」出结果面板（第一期内部知识库，第二期+网络搜索）→ 用户选中结果 → 「添加到笔记」→ 该标注附加搜索结果
- 笔记：划线+想法；标注列表页回看
- 段落锚定：标注存 `(book_id, paragraph_id, offset_start, offset_end)`——书为冻结快照，永不错位 ✅（用户编辑后才成书，成书后不改）
- 进度：阅读进度条（章节粒度），回看上次位置
- **封面复用**：抓取播客封面（og:image ✅）直接用作书封面，体验一致

**精编页（编辑可选，不挡用户）**：
- 「加入书架」一键可用（默认原稿入架，秒读）——编辑是增强不是门槛
- AI 广告标记：赞助口播/推广段落高亮 → 用户「删除/保留」一键决定
- 全文编辑：可直接改错字、删段落（用户在环上，AI 只建议不自动改）

---

## 4.4 设计规范（ins 风 · 奶油色知识空间）

> 设计原则：**产品经理作品要有设计感**——不做 SaaS 模板风，用 ins 风的轻盈与留白，传达「个人知识世界」的气质。求职展示时，设计感是区分「产品经理」与「程序员 demo」的视觉信号。

### 4.4.1 色彩系统（奶油白基调 + 低饱和灰蓝强调）
```
底色    奶油白   #F7F4EE   （比纯白有温度，全站主底）
次级底  燕麦色   #F0EBE2   （卡片/分组底）
强调色  灰蓝     #7C8FA6   （低饱和莫兰迪蓝，主强调——链接/按钮/进度/选中）
强调浅  雾蓝     #D8E0E8   （标签/背景块/词云浅色）
文字主  深灰棕   #3A3A38   （正文，不用纯黑）
文字次  灰       #8A8A86   （辅助文字）
分割线  极浅灰   #E5E0D8   （1px hairline）
警示    陶土橘   #C97B5D   （仅用于广告标记/删除等警示，少量使用）
```

### 4.4.2 线条（ins 灵魂：细、轻、少）
```
全局 hairline（1px）细线，禁用粗边框
卡片无边框，靠「柔和阴影 + 留白」区分层次
图标：outline 细线风格（stroke 1.5px），不用填充实心
分隔用极浅灰细线 + 大留白，不用重分割
```

### 4.4.3 字体（ins 灵魂：细体 + 呼吸感）
```
标题：细无衬线（思源黑体 Light / Helvetica Neue Light）
      letter-spacing 拉大（标题 +0.05em），营造轻盈精致感
正文：无衬线常规，行距 1.6-1.8（阅读舒适）
数字/数据：同字体 Light 权重，放大字号做「数据美感」
禁用：衬线标题（太书卷气）、纯黑加粗（太硬）
```

### 4.4.4 图片（胶片感，关键差异化）
```
封面（复用播客封面）：统一加柔和滤镜——低对比 + 奶油色调 + 轻微颗粒（胶片感）
词云：低饱和色块 + 大圆角，不用硬朗矩形
学习足迹热力图：莫兰迪色阶（浅灰→雾蓝→灰蓝→陶土橘），不用荧光绿
图片圆角：大圆角（16-20px），全站统一
```

### 4.4.5 布局与组件
```
首页：3 列网格（书架/词云/足迹按网格排布，ins 九宫格感）
卡片：大圆角 16-20px + 柔和阴影（低透明度、大模糊，不锐利）
留白：大量——模块间有呼吸感，不拥挤
局部深色：播放器/语义搜索页可用深色块（#2E343B 系）做对比，保留 Perplexity 科技感
```

### 4.4.6 页面气质对照
| 页面 | 气质 | 参考 |
|---|---|---|
| 首页（知识宇宙）| 轻盈、呼吸感、数据美感 | Readwise 排版 × ins 网格 |
| 阅读器 | 专注、沉浸、舒适 | Readwise Reader × 奶油调 |
| 语义搜索 | 可信、现代、可溯源 | Perplexity 深色块 × ins 细线 |
| 标注列表 | 安静、记录感 | Readwise 标注清单 |

### 4.4.7 设计验收（面试展示时自检）
- [ ] 无 SaaS 模板感（无侧边栏+顶栏+表格默认风）
- [ ] 全站统一：一套色彩/字体/圆角/线条规范
- [ ] 首页一眼看出「个人知识世界」而非「后台工具」
- [ ] 截图/录屏展示时，暗色块与奶油底对比美观

---

## 5. AI 专项

### 5.1 模型与技术选型
| 环节 | 方案 | 理由 |
|---|---|---|
| ASR 转写 | **FunASR/Paraformer（阿里开源，本地）** | 中文原生优化，优于 whisper 中文效果；本地免费、数据不出本机 |
| 摘要/金句/章节 | DeepSeek API（LLM） | 便宜（约 ¥0.01-0.03/集）；JSON mode 结构化输出 |
| 语义搜索 | 本地 embedding + 向量检索（第一期）+ SQLite FTS 兜底 | 「搜观点」是核心体验，向量检索必须第一期做；关键词兜底防漏 |
| 广告识别 | LLM 分类（提示词标记赞助段落） | 播客赞助口播模式可识别 |
| 多 Agent（二期） | LangGraph 编排（5 角色）| 多 Agent 状态流/checkpoint 需要框架；检索底层仍手写 |

### 5.2 幻觉与兜底
- **来源标注**：所有 AI 生成内容（摘要/金句/观点）标注来源（书/章节/时间戳）——可溯源
- **拒答**：语义搜索无结果时明确提示「未找到，试试换关键词」，不硬编
- **人在环上**：AI 编辑建议需用户确认（不自动改用户的书）；广告删除一键但可撤销
- **ASR 错误兜底**：提供纠错入口；用户纠错数据沉淀（第二期反哺）

### 5.3 评测方案（关键：数据从哪来）
| 评测对象 | 方案 | 数据来源 |
|---|---|---|
| 金句提取 | 对比「AI 金句」vs「用户划线」（第二期自动跑）| **用户行为 = 真值** ✅ |
| 语义搜索 | 人工标注 30-50 条「查询→期望片段」评测集；指标：Recall@K、MRR | 人工标注（少量）|
| 转写质量 | ASR 错误统计（用户纠错数据）| 用户行为 |
| 摘要质量 | LLM-as-Judge（校准后）+ 用户反馈 | 混合 |
| 阅读体验 | 划线率/笔记率/阅读完成率 | 行为埋点 |

**核心思想**：标注/纠错/搜索行为本身就是评测数据——**用户行为 = 数据飞轮**（解决 AI 产品最难的问题）。

### 5.4 成本预算
| 项 | 估算 |
|---|---|
| ASR | 本地免费；1 小时音频约转写 10-20 分钟（CPU）|
| LLM | 摘要+金句约 ¥0.01-0.03/集；实体提取（二期）¥0.1-0.3/集 |
| 存储 | SQLite 本地；1 集转写稿约 50KB，几百集几十 MB |
| 总量 | 自用规模（几十集）月成本 < ¥5 |

### 5.5 数据飞轮
```
用户划线 → 金句真值（评测 AI 金句）
用户纠错 → ASR 错误样本（反哺转写优化）
用户笔记 → 个人观点数据（听后感生成的素材）
多人划线聚合 → 共识金句（第二期）
```

---

## 6. 数据模型（核心表）

```sql
-- 节目
podcasts(id, title, author, brief, cover_url)  -- cover_url 抓取自页面 og:image ✅
-- 单集（抓取自小宇宙页面）
episodes(id, pid, eid, title, duration, pub_date,
         audio_url,  -- 音频直链（✅ 已实测 m4a），供自建播放器
         cover_url,
         shownotes_html, play_count, clap_count,
         favorite_count, comment_count, series_name,  -- 系列名从标题提取 🔵
         transcript_status)  -- pending/processing/done/failed
-- 转写稿段落
transcript_paras(id, episode_id, seq, text, start_ts, end_ts)
-- 书（冻结快照，用户编辑后生成）
books(id, episode_id, title, created_at, version)
book_chapters(id, book_id, seq, title)
book_paras(id, book_id, chapter_id, seq, text)  -- 用户编辑后的最终文本
-- 标注（锚定书的段落，永不错位）
annotations(id, book_id, book_para_id, offset_start, offset_end,
            color, note_text, created_at)
-- 向量索引
embeddings(id, source_type,  -- book_para / transcript_para
           source_id, vector, text_preview)
```

---

## 7. 技术架构（第一期）

```
[前端] Vite + React + 组件库（本地 Web UI）
[后端] Python + FastAPI
[ASR] FunASR/Paraformer（本地进程）
[LLM] DeepSeek API（JSON mode + Pydantic 校验）
[存储] SQLite（元数据/转写/书/标注）+ 本地向量文件
[检索] 本地 embedding（bge 类中文模型）+ 向量检索 + FTS 兜底
[数据源] 小宇宙分享链接抓取（curl 页面 → __NEXT_DATA__ 解析）✅
```

### 第一期工具集（内部 Function Calling）
```
tool_search(query, top_k)    → 语义搜索知识库
tool_get_episode(eid)        → 单集元数据/摘要
tool_get_book(book_id)       → 书内容/章节
tool_get_shownotes(eid)      → shownotes
```

### 第一期技术要点（新增）
```
自建播放器：HTML5 Audio + 音频直链（m4a ✅）→ 播放进度 ↔ 转写稿段落时间戳双向同步
选中搜索：选中文本 → 内部语义搜索（第一期）→ 结果附加到标注
封面：抓取 og:image ✅ 存 podcasts/episodes.cover_url → 成书时复用为书封面
```

---

## 8. 两期规划

### 第一期（MVP，目标 2-3 周）
导入 → 转写 → 加工 → 精编 → 成书 → 阅读/标注（含自建播放器+同步、选中搜索）/语义搜索
**验收**：自己用 5 本书全流程跑通（含边听边读边标注）；语义搜索 30 条评测 Recall@K ≥ 0.7

### 第二期（目标 3-4 周）
多 Agent（LangGraph 5 角色）→ 系列关联分析 → 热度分析 → 标注反哺
**验收**：3 个 Agent 任务跑通；系列关联可视化可用；金句评测闭环

---

## 9. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 小宇宙页面结构变更（抓取失效）| 导入中断 | 抓取层抽象隔离；失败提示「请稍后再试」；后续补 RSS 通道 |
| ASR 中文转写错误 | 阅读体验差 | FunASR 中文优化 + 用户纠错；术语表可自定义 🔵 |
| 4 小时音频转写时间长 | 等待焦虑 | 异步处理 + 进度条 + 完成通知 |
| 版权风险 | 法律问题 | 仅本地使用、不公开分发、不提供音频下载分发；README 声明 |
| 编辑功能过重（用户不做）| 成书率低 | 「加入书架」默认原稿一键可用，编辑可选 |
| 官方转写可用（transcript 字段）🔵 | 成本可降 | 若验证可用则优先官方转写，FunASR 兜底 |

---

## 10. 合规与伦理

- **版权**：转写稿/书仅限个人本地使用；不公开、不分发、不提供音频下载；尊重播客创作者版权
- **隐私**：数据全本地（ASR/检索本地）；LLM 调用仅发送文本片段
- **诚实边界**：AI 生成内容标注来源；不声称官方内容

---

## 11. 待确认项（评审后更新）

- [x] 北极星指标：改为「用户主动沉淀数」✅（已确认）
- [x] 播放器方案：自建播放器+转写稿同步（选 A）✅（已确认）
- [x] 选中搜索：第一期内部知识库，第二期+网络搜索 ✅（已确认）
- [x] 封面复用：播客封面作为书封面 ✅（已确认）
- [ ] 🔵 小宇宙官方转写（transcript 字段）是否可用——可用则 ASR 成本归零
- [ ] 产品名 PodLore 最终确认
- [ ] 第一期「精编页」的编辑体验：全文编辑器方案（CodeMirror/Monaco/自研）待定
- [ ] 语义搜索评测集 30-50 条的标注来源（自己标注 or 用户行为回流）
